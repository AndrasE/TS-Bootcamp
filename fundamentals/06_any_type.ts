let lessionsCount:any = 10;

lessionsCount = "Some string"

 let number:any [] = [10, "anything", true]

function  printCourse(title, lessionsCount) {
    console.log(`title = ${title}, lessionsCount = ${lessionsCount}`);
}