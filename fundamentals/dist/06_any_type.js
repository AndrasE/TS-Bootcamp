var lessionsCount = 10;
lessionsCount = "Some string";
var number = [10, "anything", true];
function printCourse(title, lessionsCount) {
    console.log("title = ".concat(title, ", lessionsCount = ").concat(lessionsCount));
}
